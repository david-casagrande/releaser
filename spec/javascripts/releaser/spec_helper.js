//= require spec_helper_lite
//= require releaser

/*
meetings.dashboard.setupForTesting();
meetings.dashboard.injectTestHelpers();

//use this instead of the default qunit adapter
Ember.Test.adapter = Ember.Test.Adapter.create({});

before(function(){
  //quick reference for accessing store
  this.store = meetings.dashboard.__container__.lookup('store:main');
});

after(function(){
  delete this.store;
  Em.run.begin();
    meetings.dashboard.destroy();
  Em.run.end();
});

beforeEach(function(){
  server = setupServer();
  meetings.dashboard.reset();
});

afterEach(function(){
  server.restore();
});
*/
